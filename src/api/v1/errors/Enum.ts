export enum SystemErrorCode {
    // ? Login
    INVALID_EMAIL = 1001,
    USER_NOT_FOUND = 1002,
    INVALID_PASSWORD = 1003,

    // ? System
    PAGE_NOT_FOUND = 5001,

    // ? General
    GENERAL_ERROR = 10000
}